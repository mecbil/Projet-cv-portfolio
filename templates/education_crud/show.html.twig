{% extends 'base.html.twig' %}

{% block title %}Educations{% endblock %}

{% block body %}
    <h1>Educations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ education.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ education.title }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ education.duree }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ education.content }}</td>
            </tr>
            <tr>
                <th>Direction</th>
                <td>{{ education.direction }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ education.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_education_crud_index') }}">back to list</a>

    <a href="{{ path('app_education_crud_edit', {'id': education.id}) }}">edit</a>

    {{ include('education_crud/_delete_form.html.twig') }}
{% endblock %}
