<form method="post" action="{{ path('app_realisations_crud_delete', {'id': realisation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ realisation.id) }}">
    <button class="btn">Delete</button>
</form>
